#include<iostream>
using namespace std;
int main(){
    int grade = 75;
    grade >=60 ? cout<<"You pass" : cout<<"you fail";
    return 0;
}

// Ternary operator expression 

// condition ? true(pass statement) : false(failed statement) ;