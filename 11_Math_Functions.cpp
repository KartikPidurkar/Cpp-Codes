#include<iostream>
#include<cmath> // for pow related functions
using namespace std;
int main(){
    double x = 3.14;
    double y = 2;
    double z;

    //z = max(x,y); // Return maximum value between two intgers or variable
    //z = min(x,y);  // Return minimum value between two intgers or varibale 
    //z = pow(2,4); // Square of number or to power of something
    //z = sqrt(9);  // square root of a number
    //z = abs(-3); // absolute value of a number
    //z = round(x); // Rounding up the value
    //z = ceil(x);   // Rounds up the value = 4
    //z = floor(x);  // Rounds down the number = 3
    cout<<z<<endl;
    return 0;
}
// Math related functions 
